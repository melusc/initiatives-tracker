{
	"name": "backend",
	"type": "module",
	"devDependencies": {
		"@lusc/tsconfig": "^4.1.0",
		"@types/better-sqlite3": "^7.6.10",
		"@types/node": "^20.14.2",
		"esbuild": "^0.21.5",
		"schemas": "workspace:^",
		"typescript": "^5.4.5",
		"xo": "^0.58.0"
	},
	"dependencies": {
		"better-sqlite3": "^11.0.0"
	},
	"scripts": {
		"fmt": "xo --fix",
		"build": "tsc && esbuild --outdir=dist --format=esm --platform=node --bundle --sourcemap --packages=external src/server.ts",
		"start": "node dist/server.js",
		"dev": "node --run build && node --run start"
	}
}
